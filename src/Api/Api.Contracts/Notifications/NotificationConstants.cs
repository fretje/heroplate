namespace Heroplate.Api.Contracts.Notifications;

public static class NotificationConstants
{
    // The name of the method that's used to send notification messages from the server to the admin
    public const string NotificationFromServer = nameof(NotificationFromServer);
}